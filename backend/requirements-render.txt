# Lightweight requirements for Render deployment (Free Tier - 512MB RAM)
# This uses CPU-only PyTorch to reduce memory usage

flask==3.1.2
flask-cors==6.0.1
python-dotenv==1.2.1

# CPU-only PyTorch (much smaller than CUDA version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

# AI and ML libraries
transformers==4.45.2
sentence-transformers==3.1.1
scikit-learn==1.5.2
numpy==1.26.4

# Document processing
pypdf==6.1.3
docx2txt==0.9

# Production server
gunicorn==23.0.0
